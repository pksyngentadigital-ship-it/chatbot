# Pinned exactly. Unpinned entries let each deploy resolve fresh versions, so
# an unrelated one-line change could pull a breaking major with no rollback
# target — pandas 3.x and starlette 1.x both already did that here once.
#
# This is the Vercel function's dependency set, and it is deliberately small:
# the bundle has a hard size limit, and every package here is imported on a
# cold start. Streamlit Community Cloud reads the same file and supplies
# streamlit itself, so it does not need to be listed.
#
# Removed in the Vercel rewrite:
#   pandas          63MB, replaced by openpyxl + the csv module
#   uvicorn         Vercel runs the ASGI server
#   jinja2          the chat shell is a static page served from the CDN
#   python-multipart, itsdangerous   no uploads and no sessions in the function
#   redis, requests unused once server-side session state is gone

fastapi==0.141.1
pinecone==9.1.0
groq==1.7.0
openpyxl==3.1.5
python-pptx==1.0.2
python-dotenv==1.2.3
